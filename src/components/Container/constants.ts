/* eslint-disable import/prefer-default-export */

import { MaxWidthType } from './ContainerPropTypes';

export const MAX_WIDTH_TYPES: { [key in MaxWidthType]: string } = {
  xl: 'Container__root--xl',
  lg: 'Container__root--lg',
  md: 'Container__root--md',
  sm: 'Container__root--sm',
  xs: 'Container__root--xs',
};
